[project]
name = "speak-easy-trainer"
version = "0.1.0"
description = "Articulation and pronunciation practice trainer"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
